{
  "name": "donemos-sc",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de agendamiento de citas para donación de sangre — Hospital Central de San Cristóbal",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.18.1",
    "pnpm": ">=9.15.0"
  },
  "scripts": {
    "dev": "echo \"Ejecuta 'pnpm --filter backend dev' y 'pnpm --filter frontend dev' en terminales separadas\" && exit 0",
    "build": "pnpm -r --if-present build",
    "test": "pnpm -r --if-present test",
    "test:smoke": "pnpm --filter backend test:integration && pnpm --filter frontend test:e2e",
    "lint": "pnpm -r --if-present lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^5.6.3"
  }
}
