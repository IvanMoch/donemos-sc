// Esquema Prisma de DonemosSC — única fuente de verdad del modelo relacional
// (research §2: Prisma 5 sobre PostgreSQL 16, BD normalizada a 3NF).
// Los modelos AdminUser, Slot, Appointment, SystemState y AdminAuditLog
// se añaden en T021 (Fase 2) conforme a data-model.md.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
